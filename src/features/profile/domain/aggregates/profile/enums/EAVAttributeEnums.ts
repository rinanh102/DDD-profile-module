export enum EAVAttributeTypes {
    NUMBER = 'number',
    STRING = 'string',
    BOOLEAN = 'boolean',
    DATETIME = 'datetime',
    ARRAY = 'array',
}

export enum EAVAttributeStatus {
    ENABLED = 'enabled',
    DISABLED = 'disabled',
}
